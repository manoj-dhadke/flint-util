---
title: "Jira:Delete Issue"
description: "Delete Jira Issue"

flintbit: "flint-util:jira:operation:delete_issue.rb"
icon: "fa fa-plus"
inputs:
  -
    section: Delete Issue
    fields:
      -
        default:
        id: issue-id
        label: Issue ID
        placeholder: "Id of issue"
        type: textbox
        required: true
      -
        default:
        id: use-proxy
        label: Use Proxy
        placeholder: "true/false"
        type: select
        required: true
        options:
          - true
          - false
      -
        default:
        id: proxy
        label: proxy
        placeholder: "proxy"
        type: textbox
        required: false
...
