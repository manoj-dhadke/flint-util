---
title: "Jira:Delete Comment"
description: "Delete comment of issue"

flintbit: "flint-util:jira:operation:delete_comment.rb"
icon: "fa fa-plus"
inputs:
  -
    section: Delete comment
    fields:
      -
        default:
        id: issue-id
        label: Issue ID
        placeholder: "Id of issue"
        type: textbox
        required: true
      -
        default:
        id: comment-id
        label: Comment ID
        placeholder: "Id of comment"
        type: textbox
        required: true
      -
        default:
        id: use-proxy
        label: Use Proxy
        placeholder: "true/false"
        type: select
        required: true
        options:
          - true
          - false
      -
        default:
        id: proxy
        label: proxy
        placeholder: "proxy"
        type: textbox
        required: false
...
