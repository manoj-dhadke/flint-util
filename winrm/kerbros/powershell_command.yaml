---
title: "Kerbros:Execute powershell command"
description: "Execute powershell command on windows machine using kerbros"

flintbit: "flint-util:winrm:kerbros:powershell_command.rb"
icon: "fa fa-terminal"
inputs:
  -
    section: Execute powershell Command
    fields:
      -
        default:
        id: hostname
        label: Hostname
        placeholder: "Enter hostname"
        type: textbox
        required: true
      -
        default:
        id: kerb5_realm
        label: Kerbros5 realm
        placeholder: "Kerbros5 realm"
        type: textbox
        required: true
      -
        default:
        id: command
        label: Command
        placeholder: "Enter Command"
        type: textbox
        required: true
      -
        default:
        id: protocol
        label: Protocol
        placeholder: "Http/Https"
        type: select
        required: true
        options:
          - "http"
          - "https"
...
