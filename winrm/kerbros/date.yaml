---
title: "Kerbros:Date"
description: "Get Date of windows machine using kerbros"

flintbit: "flint-util:winrm:kerbros:date.rb"
icon: "fa fa-calendar"
inputs:
  -
    section: Get Date of Windows Machine
    fields:
      -
        default:
        id: hostname
        label: Hostname
        placeholder: "Enter hostname"
        type: textbox
        required: true
      -
        default:
        id: kerb5_realm
        label: Kerbros5 realm
        placeholder: "Kerbros5 realm"
        type: textbox
        required: true
      -
        default:
        id: protocol
        label: Protocol
        placeholder: "Http/Https"
        type: select
        required: true
        options:
          - "http"
          - "https"
...
