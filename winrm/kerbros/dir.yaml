---
title: "Kerbros:Directory structure"
description: "Get Windows Directory structure using kerbros"

flintbit: "flint-util:winrm:kerbros:dir.rb"
icon: "fa fa-th-list"
inputs:
  -
    section: Directory Structure of Windows Machine
    fields:
      -
        default:
        id: hostname
        label: Hostname
        placeholder: "Enter hostname"
        type: hostname
        required: true
      -
        default:
        id: kerb5_realm
        label: Kerbros5 realm
        placeholder: "Kerbros5 realm"
        type: textbox
        required: true
      -
        default:
        id: protocol
        label: Protocol
        placeholder: "Http/Https"
        type: select
        required: true
        options:
          - "http"
          - "https"
...
