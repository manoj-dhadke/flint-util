---
title: "NTLM:Execute powershell command"
description: "Execute powershell command on Windows machine using NTLM"

flintbit: "flint-util:winrm:ntlm:powershell_command.rb"
icon: "fa fa-terminal"
inputs:
  -
    section: Execute powershell command on Windows machine
    fields:
      -
        default:
        id: protocol
        label: Protocol
        options:
          - "http"
          - "https"
        placeholder: "http/https"
        type: select
        required: true
      -
        default:
        id: hostname
        label: Hostname
        placeholder: "Enter Hostname"
        type: textbox
        required: true
      -
        default:
        id: username
        label: Username
        placeholder: "Enter Username"
        type: textbox
        required: true
      -
        default:
        id: password
        label: Password
        placeholder: "Enter Password"
        type: password
        required: true

...
