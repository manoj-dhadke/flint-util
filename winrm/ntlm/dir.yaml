---
title: "Windows Directory structure"
description: "Get windows Directory structure"

flintbit: "flint-util:winrm:ntlm:dir.rb"
icon: "fa fa-th-list"
inputs:
  -
    section: Get windows Directory structure
    fields:
      -
        default:
        id: protocol
        label: Protocol
        options:
          - "http"
          - "https"
        placeholder: "http/https"
        type: select
        required: true
      -
        default:
        id: hostname
        label: Hostname
        placeholder: "Enter Hostname"
        type: textbox
        required: true
      -
        default:
        id: username
        label: Username
        placeholder: "Enter Username"
        type: textbox
        required: true
      -
        default:
        id: password
        label: Password
        placeholder: "Enter Password"
        type: password
        required: true

...
